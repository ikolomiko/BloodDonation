@using Microsoft.AspNetCore.Html;

@{
    ViewData["Title"] = "Kan İhtiyacı Listesi";

    HtmlString GenerateTd(string? s)
    {
        if (s == null)
        {
            return new HtmlString("<td style=\"color:gray\"> - </td>");
        }
        else
        {
            return new HtmlString($"<td>{s}</td>");
        }
    }
}

@model List<BloodDonation.Web.Models.NeedForBlood.ListAllViewModel>

<div class="row">
    <div class="col-lg-12">
        <div class="">
            <h4 class="card-header text-center bg-white">
                Kan İhtiyacı Listesi (Bütün Hastaneler, Bütün Kan Grupları)
            </h4>
            <div class="card-body">

                <div class="container mb-3 p-0">
                    <button type="button" class="btn btn-success" onclick="location.href='@Url.Action("Add", "NeedForBlood")'">
                        Yeni Kan İhtiyacı Ekle
                    </button>
                </div>

                <div class="table-responsive">
                    <table class="table table-responsive table-bordered table-striped">
                        <thead>
                            <tr>
                                <th>
                                    ID
                                </th>
                                <th>
                                    Hastane Adı
                                </th>
                                <th>
                                    Kan Grubu
                                </th>
                                <th>
                                    İhtiyaç Duyulan Miktar (Ünite)
                                </th>
                                <th>
                                    Eylemler
                                </th>
                            </tr>
                        </thead>
                        <tbody>


                            @foreach (var item in Model)
                            {
                                <tr class="align-middle">
                                    @GenerateTd(item.Id.ToString())
                                    @GenerateTd(item.HospitalName)
                                    @GenerateTd(item.BloodGroupName)
                                    @GenerateTd(item.Amount.ToString())
                                    <td class="text-nowrap text-center">
                                        <button type="button" class="btn btn-sm btn-warning mx-1 px-2">
                                            Düzenle
                                        </button>
                                        <button type="button" class="btn btn-sm btn-danger mx-1 px-3">
                                            Sil
                                        </button>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>

                @if (!string.IsNullOrEmpty(ViewBag.ErrorMessage))
                {
                    <div class="text-danger">
                        @ViewBag.ErrorMessage
                    </div>
                }
                @if (ViewBag.ErrorMessageList != null)
                {
                    <div class="text-danger">
                        @foreach (var item in ViewBag.ErrorMessageList)
                        {
                            <span>@item</span>
                            <br />
                        }
                    </div>
                }

            </div>
        </div>
    </div>
</div>